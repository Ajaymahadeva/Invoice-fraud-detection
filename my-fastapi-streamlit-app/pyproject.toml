[tool.poetry]
name = "my-fastapi-streamlit-app"
version = "0.1.0"
description = "A web application using FastAPI for the backend and Streamlit for the frontend."
authors = ["Your Name <your.email@example.com>"]
license = "MIT"

[tool.poetry.dependencies]
python = "^3.8"
fastapi = "^0.75.0"
uvicorn = "^0.17.0"
python-dotenv = "^0.19.0"
google-genai = "^0.1.0"
streamlit = "^1.10.0"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"